{#
/**
 * @file
 * This file is for sample data and styling only.
 * Markup will be provided by CKEditor
 */
#}

{{ attach_library('wcm8_patterns/callout') }}


{%
  set classes = [
    'paragraph',
    'paragraph--type--' ~ paragraph.bundle|clean_class,
    view_mode ? 'paragraph--view-mode--' ~ view_mode|clean_class,
    not paragraph.isPublished() ? 'paragraph--unpublished'
  ]
%}

{% block paragraph %}
  <div{{ attributes.addClass(classes) }}>
    {% block content %}
      <div class="callout callout--{{ callout_type }}">
        <div class="callout-icon">
          <span class="visually-hidden">UX Tip</span><i class="fas {{ callout_icon }}"></i>
        </div>
        <div class="callout-body">
          {{ content }}
        </div>
      </div>
    {% endblock %}
  </div>
{% endblock paragraph %}
